{
  "rules": {
    "presence": {
      "$quizId": {
        ".read": true,
        "$participantId": {
          ".write": "!newData.exists() || (newData.hasChildren(['state']) && newData.child('state').val() == 'online')",
          ".validate": "newData.hasChildren(['state', 'joinedAt']) && newData.child('state').isString() && newData.child('joinedAt').isNumber()"
        }
      }
    },
    "reactions": {
      "$quizId": {
        ".read": true,
        "$reactionId": {
          ".write": "!newData.exists() || (!data.exists() && newData.hasChildren(['emoji', 'participantId', 'createdAt']))",
          ".validate": "newData.hasChildren(['emoji', 'participantId', 'createdAt']) && newData.child('emoji').isString() && (newData.child('emoji').val() == '👍' || newData.child('emoji').val() == '🔥' || newData.child('emoji').val() == '😂' || newData.child('emoji').val() == '😮' || newData.child('emoji').val() == '👏' || (newData.child('emoji').val().length >= 1 && newData.child('emoji').val().length <= 4)) && newData.child('participantId').isString() && newData.child('createdAt').isNumber()"
        }
      }
    }
  }
}
